# Copia este archivo como .env para producción/builds personalizados.
# En desarrollo NO es necesario: Vite usa el proxy '/api' por defecto.

# URL base de la API en producción (sin barra final), por ejemplo:
# VITE_API_URL=https://api.tudominio.com/api/v1
VITE_API_URL=/api/v1
